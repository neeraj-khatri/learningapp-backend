const express = require('express');
const router = express.Router();

const {signupHandler,testHandler} = require('../controller/controller');


router.post('/signup', signupHandler);
router.get('/test', testHandler);

module.exports = router;